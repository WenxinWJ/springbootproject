server:
  port: 8086
  servlet:
    context-path:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/employee?useSSL=false&serverTimezone=Asia/Shanghai
    username: king
    password: lian

# 安全组件的配置
  jackson:
    serialization:
      indent_output: true
#redis
  redis:
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-active: 8
        max-wait: -1ms
#将日志信息加入mongoDB
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/logs



mybatis:
  type-aliases-package: com.springbootproject.domain
  mapper-locations: classpath:mybatis/*.xml

# 日志
logging:
  level:
    org.springframwork.security: info
    com.springbootproject.mapper: debug

#Jwt
jwt:
  header: Authorization
  secret: mySecre
  #token
  expiration: 604800
  route:
    authentication:
      path: /auth
      refresh: /refresh


